# 速览与行动清单（15 分钟版）

> 完整设计文档是 `C++核心协同方案.md`（2720 行）。**不用先看它。**
> 本文只回答三件事：
> **① 这东西怎么工作（5 分钟） ② 现在代码到哪一步了 ③ 接下来谁做什么、怎么算做完**

---

## 1. 五分钟看懂这个系统

### 1.1 一句话

**把音乐变速，让它的拍子踩在你的脚步上。**

你在跑，步频会变（上坡、累了、冲刺）。音乐实时加减速，让自己的鼓点一直落在你脚上。

### 1.2 核心只有一条规则

把**音乐的节拍**想成一个转盘上的指针——每来一拍转一圈；**你的脚步**是另一个转盘——每迈一步转一圈。

```
        音乐转盘                    脚步转盘
          ╭───╮                      ╭───╮
        ╱   ↑   ╲                  ╱   ↑   ╲
       │    │    │                │    │    │
        ╲   │   ╱                  ╲   │   ╱
          ╰───╯                      ╰───╯
       一拍转一圈                    一步转一圈
```

**两根指针的夹角 = 音乐和脚步错开了多少。**

于是整件事只剩两条规律：

| 规律 | 大白话 |
|---|---|
| **转速相同 → 夹角冻住** | 倍速 = 你的步频 ÷ 歌曲 BPM，夹角就永远不变。**这就是"对齐"的定义。** |
| **转速差 1% → 每步错开 0.02 → 8 秒后错开半个拍** | 所以精度要求很苛刻，必须一直微调 |

**⇒ 全部算法都在干同一件事：让两个转盘的转速一样，并盯着夹角。**

### 1.3 三种播放模式

| 模式 | 速度从哪来 | 谁跟着谁 | 难点 |
|---|---|---|---|
| **步频跟随** | 你**实测**的步频 | 音乐跟脚步 | 步频一直变，必须闭环追 |
| **恒定 BPM** | 规定一个数字（如 170） | 脚步跟音乐 | 起播时"夹角"无从得知 |
| **曲线** | 你画的曲线（热身 150 → 目标 170 → 放松） | 脚步跟音乐 | 同上 |

> 后两种是**开环**：算倍速用的步频 = 目标步频，所以**夹角一旦定下就永远不漂**，
> 根本不需要闭环控制。要解决的只有"最开始那一下夹角的对齐"。

### 1.4 三个 C++ 核心各干什么

```
加速度计(50Hz)
   │
   ▼
┌──────────────────┐   落脚的"墙钟时刻 + 步频"
│ step_detector.hpp│ ─────────────────────────┐
│ 自适应阈值 + 谷峰 │                          │
└──────────────────┘                          ▼
                                    ┌────────────────────┐
歌曲文件 ─► Essentia 分析           │  TempoFollower.hpp │
           (BPM / 拍点 / 段落)      │  ADJUST→CHASE→HOLD │
                    │               └─────────┬──────────┘
                    │                         │ 倍速 m
                    ▼                         ▼
            ┌────────────────┐      ┌──────────────────────┐
            │ CppDataAnalyzer│      │ LiveStretchPlayer.h  │
            │ 段落 BPM / 拍网格│ ───► │ 保持音高实时变速      │
            └────────────────┘      └──────────────────────┘
                                              │
                                              ▼
                                        声卡 / 耳机
```

---

## 2. 现在的代码到哪一步了

### 2.1 ★ 最重要的一张表：哪些编进了 App

`CMakeLists.txt` **只编三个源文件**：`napi_bridge_center.cpp`、`test_audio.cpp`、`audio_process.cpp`。其余全是"零件摆在那儿，但没接上线"。

| 文件 | 进构建？ | 状态 |
|---|---|---|
| `napi_bridge_center.cpp` | ✅ | NAPI 桥 + 播放调度，App 唯一入口 |
| `audio_process.cpp` | ✅ | FFmpeg 解码成 float PCM |
| `test_audio.cpp` | ✅ | 冒烟测试，无业务 |
| `EssentiaBeats.hpp` | ✅ 间接 | 被 NAPI 引入，能用：出 BPM、拍点 CSV |
| `LiveStretchPlayer.h` | ✅ 间接 | 被 NAPI 引入，能用：变速播放 |
| `include/TimeStretchEngine.hpp` | ✅ 间接 | 变速引擎本体，质量高 |
| **`step_detector.hpp`** | ❌ | **全工程无一处 include** |
| **`TempoFollower.hpp`** | ❌ | **全工程无一处 include** |
| **`CppDataAnalyzer.hpp`** | ❌ | **全工程无一处 include** |
| `heavyWorkTest.cpp` | ❌ | 独立教学程序（NAPI TSFN 模板） |
| `stretchbridge.cpp` | ❌ | 独立测试程序（不编入 App） |

> **⇒ 一句话总结：三个核心算法（计步 / 跟随 / 段落分析）都写完并且验证过了，但一行都没接进 App。**

### 2.2 三个核心头文件的验证状态

下面这三个都已经**单独验证过**（各人用自己手边的验证方式跑的，**那些脚手架都不入库**），可以直接用：

| 头文件 | 验证结论 |
|---|---|
| `step_detector.hpp` | 自测 26/26 通过；与独立的 Python 镜像实现逐样本偏差 `0.00e+00` |
| `TempoFollower.hpp` | 冷启动 2.83 秒进锁定；长跑日志零报错 |
| `LiveStretchPlayer.h` / `TimeStretchEngine.hpp` | 引擎延迟恒定 120 ms、seek 零误差、暂停恢复相位连续 |

> ⚠️ **如果有人手边还有一份旧的 `LiveStretchPlayer.h`，不要用。**
> 判据：`stop()` 里带一句 `if (!running_) return;`。
> 自然播放结束后 `running_ == false` 但工作线程仍然 joinable，这句守卫会让 `stop()` 永不 `join()`，
> 随后重播（给 joinable 的 thread 赋值）或析构时直接 `std::terminate()`。
> **工程里的这份已经删掉了那行 —— 一律以工程为准。**
>
> 📌 **验证用的脚手架（GUI / 桥接 / pybind / 各种测试台）都不入库**，只有 `.hpp` 头文件是交付物。

---

## 3. 接下来做什么

### 3.1 ⚠️ 先拍三个板（不拍会返工）

| # | 决定 | 影响 |
|---|---|---|
| **①** | **歌库是预置/可运营的，还是要支持用户导入本地音乐、在设备上离线分析？** | 决定 downbeat（小节相位）走**服务端**还是**设备端算法**，两者工作量差 3 倍 |
| **②** | **v1 做哪几个模式？**（跟随 / 恒定 BPM / 曲线） | 决定任务 3 要不要做 |
| **③** | **接歌（交叉淡化）在不在 v1？** | 决定任务 7 和 Mixer 改造要不要做 |

其余 11 条待定项（参数单位、八度层级、滑条……）**不阻塞，边做边定**。
### 3.2 任务清单（按依赖排序，可直接认领）

> **三个前提**（2026-09 更新）
> 1. **当前没有运行环境** —— 需要真机、需要实测标定、需要真实素材调参的任务先跳过。
> 2. **测试脚手架不会入库** —— GUI / 桥接 / pybind / 各种测试台都是用完即弃的，只有 `.hpp` 头文件是交付物。
> 3. **★ 职责边界**：`TempoFollower` 只管**动态模式**（倍速 = 实测步频 / BPM）；
>    `PhaseTrim` 只管**预设模式**（倍速 = 规定步频 / BPM，即恒定 BPM 与曲线）。
>    **同一时刻只能有一个"相位所有者"** —— 动态模式下不要再叠 PhaseTrim，两个控制器抢同一个变量会打架。

| 任务 | 本阶段 | 说明 |
|---|---|---|
| **0** `beatgrid.hpp` | ✅ **已入工程** | 拍点 / 小节网格。`entry/src/main/cpp/beatgrid.hpp` |
| **2b** `phase_trim.hpp` | ✅ **已完成** | 预设模式的相位同步工具，**待搬运** |
| **2a** 延迟换算 | 🟢 可做 | 纯算法，验收靠文档里的自测向量 |
| **3** 开环速度来源 | 🟢 可做 | 纯逻辑，无依赖 |
| **4** `switch_plan.hpp` | ✅ **已入工程** | 切歌对拍，依赖任务 0 |
| **5** 锚点圆均值 | 🟢 可做 | 纯算法，可用合成拍点自测 |
| **7a** `planMix` | 🟢 可做 | 纯逻辑，依赖任务 0 |
| **1** 接主线 | 🔴 跳过 | 需要真机（OH_SENSOR / OH_AudioRenderer） |
| **2c** 标定值 | 🔴 跳过 | 必须实测输出延迟与生物力学偏移 |
| **6** downbeat | 🔴 跳过 | 特征权重必须拿真实音乐调 |
| **7b** Mixer | 🔴 跳过 | 需要能出声 / 渲染的环境 |

---

#### ✅ 任务 0 · `beatgrid.hpp` ｜**已入工程**

**交付**：纯头文件 · C++11 · 零依赖 · 320 行。**怎么用 / 在哪用**见 `docs/BeatGrid_技术文档.md`。

| 验收项 | 结果 |
|---|---|
| C++ 自测（10 组 80 项） | **全过** |
| 与 `tempo::beatOffset()` 逐值比对 | 未规范化时 30000 组**逐位相等**；需规范化时 40000 组 maxDiff `3.04e-13`（浮点上界内） |
| 三方交叉验证（C++ 网格 / C++ 参考 / Python 镜像） | 全部 `0.000e+00`（位级一致） |
| `-std=c++11 -Werror -pedantic` | 通过（工程里是 C++11） |
| 负锚点取点永不为负 | 6000 组，**0 次**返回负数 |
| 小节网格 | 1500 组随机 BPM / 锚点 / 拍号 / 小节相位，全过 |

---

#### ✅ 任务 2b · `phase_trim.hpp` ｜**已完成** ｜ 依赖任务 0

**它是什么**：预设模式（恒定 BPM / 曲线）的**相位同步工具**。做法是给倍速叠一个有界、双向、临时的偏差 δ，
把实测相位折回 0，**而平均速度不变**（δ 最后一定收回 0）。

**★ 默认就是伺服模式**（`guardOnly = false`）—— 因为它的本职是"对齐"。
护栏模式（`guardOnly = true`）是可选件，**修不了 0.5 这种偏置**（0.5 < guardAt 0.60），不要拿它当同步工具。

**关键保证**：`abs(δ) <= deltaMax`；变化率不超过 `slewPerSec × dt`；
不介入时 δ 归零 ⇒ 长期平均速度严格等于规定倍速；
`enabled = false` 时 δ 会**平滑收回 0**，不会把音乐永久留在错误倍速上。

| 验收项 | 结果 |
|---|---|
| C++ 自测（9 组 68 项） | **全过** |
| 限幅 / 限速 | 4000 组随机参数 + 随机 err，δ 与变化率都没越界 |
| 闭环收敛（走真实漂移动力学 `dr/dt = 2·δ·(C/60)`） | 0.5 的偏置 **4.34 秒**拉回；0.9 的偏置 **5.88 秒**；所有场景最终 δ = 0 |
| 饱和搬运速率 | `2 × 0.02 × C/60`；180 步/分 = **0.12 offset 单位/秒**（与文档 §8.6「≈5.6 秒拉回 1/4 拍」吻合） |
| 方向约定 | err > 0 ⇒ δ < 0（减慢），2000 组随机验证 |

**⚠️ 已知边界**：用户大幅偏离曲线时（比如快 9%），相位以约 0.5 单位/秒 扫过，
而 δ 只能提供 0.12 单位/秒 —— **两种模式都挡不住**。这是预设模式的本分（音乐是主，人跟着跑），
想真正跟住用户请改用动态模式。

---

#### 任务 1 · 接上主线：传感器 → 计步 → 跟随 → 变速 ｜无依赖 ｜ 1~2 天 ｜ ★最高优先级

**做什么**：ArkTS 每 100 ms 送一批加速度数据进 NAPI，喂 `StepDetector`；脚步回调里算歌曲位置喂 `TempoFollower`；每批末尾取 `currentMultiplier()` 推给 `LiveStretchPlayer::setSpeed()`。

**为什么最重要**：**这是"能不能用"的底线。** 做完这一步就能演示"音乐跟着步频变速"。

**交付**：改 `napi_bridge_center.cpp`，新增 2~3 个 NAPI 接口。

**验收**：
- 手机上走两步，日志能看到步频、倍速随步频变化；
- 换成跑步，倍速跟得上；
- 停下来，音乐保持最后一档速度不抽风。

**坑**：**必须用检测器回填的 `e.timestamp` 反查歌曲位置**，不能直接读"现在"的位置。详见 §4 坑①。

**代码**：完整版 §11.1 / §11.2。

---

#### 任务 2 · 延迟标定 + 对齐微调滑条 ｜依赖任务 1 ｜ 1 天

**做什么**：量一次端到端延迟（输出延迟 + 生物力学偏移），换算成 `stepDelaySec`；界面上加一个 ±1/8 拍的对齐滑条。

**验收**：完整版 §11.3 里那 5 组自测向量当单测；真机上拖滑条能听出差别。

**代码**：§11.3 + §11.5。

---

#### 任务 3 · 曲线 / 恒定 BPM 模式 ｜依赖任务 1 ｜ 1 天 ｜ 看决定②

**做什么**：速度不再来自 follower，改成 `倍速 = 曲线(运动时间) ÷ 该时刻的歌曲 BPM`；曲线时间轴在暂停时**冻结**。

**验收**：切曲线段倍速跟着变；暂停 30 秒再恢复，相位不跳。

**代码**：§11.6。

---

#### ✅ 任务 4 · 切歌对拍 ｜**已入工程** ｜ 依赖任务 0

**它是什么**：换歌时算出**什么时候起播 B、B 从哪一秒开始播**，让 B 的拍点接上 A 的拍点。
速度侧自动就是一样的（两首歌的墙钟拍周期都是 `60/步频`），所以只需要管相位。

**接口**：`bpmw::planSwitch(SwitchRequest)` → `SwitchPlan{tEdge, tStart, seekFrame, leadBeats, ...}`。
用请求结构体而不是 12 个位置参数（后者太容易传错顺序）。

**怎么用 / 在哪用**：见 **`docs/SwitchPlan_技术文档.md`**。

| 验收项 | 结果 |
|---|---|
| C++ 自测（9 组 9024 项） | **全过** |
| 跨切点拍点落在同一格子 | 3000 组随机场景 |
| `tEdge` 正好是 A 的下一拍 | 2000 组 |
| 整拍不变性 / 顺延整拍 | 各 1500 组 |
| 负锚点 + 极端 BPM（40~400） | 1500 + 6 组 |
| 端到端「合并后是一条等距拍点列」 | 800 组 |
| 降级路径（B 无网格） | 1000 组 |

**未做**：真机端到端（需要运行环境）—— 目前只能验算法与相位关系。

---

#### 任务 5 · 锚点改用"相位圆均值" ｜无依赖 ｜ 1 天 ｜ 可并行

**做什么**：分析阶段不要再用回归截距当"第一拍时间"，改用所有拍点的相位圆均值；对负值加质量闸门。

**为什么**：**投入产出比最高、风险最低的一条。** 顺手解决"第一拍为负导致 seek 失败"和"两首歌各自有 ±30 ms 锚点误差"。

**验收**：旧 CSV 与新 CSV 算出的网格相位一致；负锚点的歌曲不再 seek 失败。

**代码**：§11.10。

---

#### 任务 6 · 小节相位（downbeat） ｜★取决于决定① ｜ 2~5 天

**做什么**：
- 歌库预置 ⇒ **服务端跑 madmom，把 `bar_phase` 写进 CSV**，设备端只读一个整数；
- 必须设备端 ⇒ 自研"每拍特征 + 4 态 Viterbi"，设备端纯 C++。

**关键认知**：**接歌要的是"一致性"，不是"正确性"。** 只要两首歌用同一套算法，就算都判错，混起来也照样对。

**验收**：人工听 10 首歌，`bar_phase` 与耳朵判断一致率 > 80%。

**代码**：§11.9（含完整的 4 态 Viterbi）。

---

#### 任务 7 · 接歌（交叉淡化） ｜★取决于决定③ ｜ 3~5 天

**做什么**：在 A 的**小节线**上开始淡出、同时 B 从它自己的小节线进入。需要一个**单线程 Mixer** 同时拉两个 engine。

**验收**：混音输出 WAV，跨混音点的拍点间隔全部 = T。

**代码**：§11.8。

---

### 3.3 依赖关系与里程碑

```
任务 0 (beatgrid)  ──┐
                     ├──► 任务 4 (切歌) ──┐
任务 1 (接主线) ★ ───┤                    ├──► 任务 7 (接歌)
        │            └──► 任务 2/3        │
        │                                 │
任务 5 (锚点) ────────────────────────────┤
任务 6 (小节相位) ★ ──────────────────────┘
```

| 里程碑 | 内容 | 累计 |
|---|---|---|
| **M1** | ~~任务 0~~（✅ 已完成）+ 任务 1：**能演示"音乐跟着步频变速"** | 1~2 天 |
| **M2** | + 任务 2、3：三种模式齐全 + 对齐可调 | 4 天 |
| **M3** | ~~任务 4~~（✅ 已完成）：**切歌不断拍** | — |
| **M4** | + 任务 5、6、7：段落对齐 + 接歌 | 看决定①③ |

**⇒ 最快 2 天就有能演示的东西。先做 M1。**

---

## 4. 五条"不看会踩"的坑

### ① 脚步的歌曲位置必须"回填"，不能读"现在"

`StepEvent.timestamp` 是**波峰时刻**（= 落脚时刻），不是上报时刻。如果你在收到回调的瞬间去读 `getInputPosition()`，就等于把几十毫秒**抖动**加回去了 —— 而"完美"窗口只容得下 17~25 ms 抖动。

```cpp
// ❌ 错
double songSec = getInputPosition() / sampleRate;

// ✅ 对（用回填时间戳反查）
double songSec = songSecAt(e.timestamp, m);   // §11.1
```

### ② `stepDelaySec` 的单位是"歌曲秒"，不是"墙钟秒"

现有实现的换算是 `stepDelaySec × B / 30`，**缺了一个 m\* 倍**（最多差一倍）。写上层参数前先决定用哪个单位，建议**直接用 offset 单位**（界面上它就是"对齐微调"）。

### ③ `firstBeatSec` 为负：**取模，不要 clamp**

第一拍算出负数是**弱起，完全正常**。直接 clamp 到 0 会把**整个网格平移**（180 步/分下 = 0.72 个单位，比整个"好"窗口还大），症状是"听起来差了半拍"但代码每行都对。

```cpp
anchor = f - P * floor(f / P);   // ✅ 网格不变，且 ≥ 0
// anchor = max(0.0, f);         // ❌ 网格被平移了
```

### ④ 旧版 `LiveStretchPlayer.h` 不要用

判据：`stop()` 里带一句 `if (!running_) return;`。自然播放结束后 `running_ == false` 但线程仍 joinable，
这句守卫会让 `stop()` 永不 `join()`，随后重播或析构时直接 `std::terminate()`。
**工程里的这份已经删掉了那行 —— 一律以工程为准。**

（`stretchbridge.cpp` 工程里那份的帧计数还是 `int`，有更新版的话可以合一下；但它本来就不编进 App，优先级低。）

### ⑤ 同一时刻只允许一个"速度来源"

两首歌同时在响（接歌）时，必须由**同一个**倍速值驱动，否则网格立刻错开。
另外两个 `LiveStretchPlayer` **不能各自起线程排拍**（会相对漂移），要改成一个 Mixer 线程拉两个 engine。

---

## 5. 验收总表（一页）

| # | 验证什么 | 怎么做 | 通过标准 |
|---|---|---|---|
| 1 | `Δ_sw`（切歌路径固定延迟） | `stretchbridge dump` 渲染前 0.5 秒，与稳态路径比首帧 | 得到稳定的常数 |
| 2 | 回填时间戳 vs 不回填 | 同一段合成步态跑两遍 `tempo_selftest` | 回填版 goodRate 明显更高 |
| 3 | 延迟耐受 | 人为叠加恒定延迟 0/40/83/167/250/333 ms | 与预期吻合（**167→半拍，333→0**） |
| 4 | 抖动耐受 | 叠加 σ = 10/20/30/50 ms 抖动 | 找出 HOLD 崩掉的阈值 |
| 5 | 切歌相位连续 | 两段带拍标记的音频按 §11.7 切，输出 WAV 数间隔 | **跨切点间隔全部 = T** |
| 6 | 整拍不变性 | 入点整拍平移 k=1,2,3 | 输出完全相同 |
| 7 | 八度一致性 | 同一首歌用 90/180 BPM 两个 CSV 各切一次 | 感知速率不变 |

---

## 6. 完整版索引（要查细节时再看）

工程里现在有五份文档，分工是：

| 文档 | 什么时候看 |
|---|---|
| **`速览与行动清单.md`**（本文） | 想知道**接下来做什么**、谁做、怎么算做完 |
| **`BeatGrid_技术文档.md`** | 要用 `beatgrid.hpp` 时看 —— 拍点/小节网格怎么调、在哪用 |
| **`PhaseTrim_技术文档.md`** | 要用 `phase_trim.hpp` 时看 —— 预设模式的相位同步工具 |
| **`SwitchPlan_技术文档.md`** | 要用 `switch_plan.hpp` 时看 —— 切歌对拍 |
| **`C++核心协同方案.md`** | 要查**原理和公式**时看（2720 行，别通读） |

---

| 想知道 | 去看 |
|---|---|
| **`beatgrid.hpp` 怎么用 / 在哪用** | **`BeatGrid_技术文档.md`** |
| **`phase_trim.hpp` 怎么用 / 在哪用** | **`PhaseTrim_技术文档.md`** |
| **`switch_plan.hpp` 怎么用 / 在哪用** | **`SwitchPlan_技术文档.md`** |
| 动态模式 vs 预设模式该用哪个 | `PhaseTrim_技术文档.md` §2（职责边界） |
| 两个转盘 / 夹角 / 为什么延迟不可怕 | `C++核心协同方案.md` §0.5（零数学版，可单独读） |
| 相位漂移公式、精度要求怎么来的 | §2 |
| 延迟怎么标定、符号为什么相反 | §3.6 ~ §3.8 |
| 切歌的完整推导 | §4 |
| 三种模式的差别 | §8 |
| Essentia 到底有没有 downbeat（**没有**，已核实） | §9.5 |
| 负锚点为什么不能 clamp | §10 |
| **全部代码片段** | **§11**（11 个可直接落地的模块） |
| 还有哪些待定项 | §12 |

